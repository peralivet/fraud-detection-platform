# Models Module

This module contains model training, model loading, and model persistence logic.

Responsibilities may include:

- training baseline models
- training advanced fraud models
- saving trained model artifacts
- loading models for inference
- defining model interfaces

This module should focus on model lifecycle logic, not raw data ingestion.
